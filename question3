const sumOfTwo = (arr, k) => {
    for (let i = 0; i < arr.length; i++) {
        for (let j = 0; j < arr.length; j++) {
            if (j != i && arr[i] + arr[j] === k) {
                return true;
            } 
        }
    }
    return false;
}


O(n^2)